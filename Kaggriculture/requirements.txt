# Core loop (analysis/, evaluation/, game/stub_env.py) has no third-party
# dependencies beyond the standard library.

# Optional: enables agents/llm_client.py's unattended API mode.
# Without this installed (and ANTHROPIC_API_KEY set), the loop runs in
# manual mode and writes prompts to reports/pending_prompts/ instead.
anthropic>=0.40
